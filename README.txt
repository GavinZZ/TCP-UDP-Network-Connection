This assignment is written in Python 3.7 --- client.py, server.py.

Step to test:
1. Run the server script, './server.sh <req_code>', where <req_code> should be a positive number
2. The server script will print a value, which is the <n_port> value
3. Run the client script, './client.sh <server address> <n_port> <req_code> <text>'

Note:
